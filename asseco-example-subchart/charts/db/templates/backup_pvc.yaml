apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "name" . }}-backup
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.backupStorageSize }}
